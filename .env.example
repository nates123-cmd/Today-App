VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
# Shared secret for the `claude` edge fn (matches STOCK_PROXY_SECRET in
# supabase secrets). When unset, useHealthInsight falls back to static lines.
VITE_CLAUDE_PROXY_SECRET=
